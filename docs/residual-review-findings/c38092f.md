# Residual review findings — c38092f (issue #70)

**Status: review panel DID NOT RETURN — recorded as a residual, not silently skipped.**

Four reviewer sub-agents were spawned for the #70 diff (correctness, simplicity,
data-integrity lenses; three async + one synchronous fallback). None delivered a
report within the session — the completion-notification relay never fired despite
pings. Per Rule 12 the panel is recorded as NOT RUN rather than claimed green;
the issue card stays at `status:review` until a human (or a follow-up session)
re-runs the panel or reviews the commit directly.

## What WAS verified without the panel (by the building agent)

- Validation contract green: repo pytest 1329 passed (sole failure = the known
  inherited `tests/agents/worker/test_interview_route.py::test_deeper_turn_returns_terminal_with_gemini_mocked`,
  pre-existing on this branch), `npx vitest run` 724 passed, ruff clean.
- Replay regression suite green: `tests/agents/pipeline/test_shortlist_replay_2026_07_25.py`
  (18 tests) replays all 15 scrambled 07-25 audit rows through the real chain.
- Panel target-risk areas self-checked in code:
  - every `assign_category` caller compiles with the new optional
    `theme_category_by_story` param; no positional misalignment (reconcile passes
    it 5th-positional deliberately).
  - call sites NOT threaded with the theme map (ranking classify, feed-assembly
    beyond-bubble) cannot regress for produced data: a zero-keyword-tag story is
    gated out at `SKIP_REASON_NO_INTEREST` (produce_gate check 1) and never
    reaches feeds.
  - reconcile's new pin uses the RECONCILED tag index + merged-story themes;
    `provisional_categories` is index-aligned to the original pool and indexed by
    a member of that pool.
  - `(story_id, interest_id)` duplicates impossible: `merge_story_tags` dedups per
    interest; `_remap_tags` dedups per edge.
  - `theme_category.py` memoization is over a pure function; bounded (4096).

## Known residuals (deferred deliberately, not defects of this commit)

1. **Historical prod rows**: `story_interests` rows persisted by PRE-#70 runs still
   contain theme ROOT tags at depth 0 and shifted keyword depths. Read paths over
   old rows (worker feed path `agents/worker/pipeline_routes.py`, memory
   `session_processor`) will see the old shape until those stories age out or a
   backfill runs. Overlaps the open worker-path issues #65/#66/#67 — noted there,
   not fixed here (Rule 3).
2. **Lexical-only mode**: with `ENABLE_SEMANTIC_RELEVANCE_KEY` off, a lexical
   false-positive fetch now categorizes by its (wrong) fetching interest — the old
   M2 case. Accepted trade: the live batch runs with the semantic key ON; the
   two-key lock is the designed relevance guard (#50/#51).
3. **Audit-doc field-semantics note**: `shortlist_matched_interest_slugs` now
   reports verified fetching leaves only (theme roots never appear). The shortlist
   artifact schema (field names/types) is UNCHANGED, so audit tooling keeps
   parsing; the next audit should expect leaf slugs, not roots.
