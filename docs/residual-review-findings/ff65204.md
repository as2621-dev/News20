# Residual review findings — ff65204 (slice #10 day-1)

Panel: correctness/logic + simplicity over the slice diff. No FIX-NOW findings; the
following DEFERs are recorded so they are not silently lost.

1. **Interview slug depth cap not enforced in guards.** `_is_valid_slug`
   (`agents/interview/guards.py`) checks root anchoring + segment shape but not the
   ≤ 4-segment depth cap that `scripts/seed_personas.py` / the mint conventions
   (`reference/interview-onboarding-spec.md` §3) assume. **Pre-existing** (not
   introduced by the normalization change). Risk: a model-proposed 5-deep slug under a
   lit root would validate and mint deeper than the taxonomy contract. Small guard
   addition + test when next touching the interview engine.

2. **`ONLY_USER_EMAIL=","` (separators only) now scopes to zero users silently**
   (`scripts/run_live_batch.py`) instead of erroring like the old single-email path.
   Degrades to an empty preflight ("no active users" FAIL) rather than a crash —
   acceptable, but a loud "no valid emails in ONLY_USER_EMAIL" message would be
   friendlier. Cosmetic.

3. **`_normalize_slug` pathological-input edge** (`"ai.---.models"` → empty segment)
   is correctly rejected by the segment regex but has no dedicated test. Add one for
   completeness alongside (1).
