# Residual review findings — 8b85667 (issue #36, queryless-interest sweep + loud-fail)

Panel: correctness, simplicity/reuse, data-integrity — all three returned (no gaps).

## Applied in the follow-up commit
- **medium · SQL queryless predicate vs pipeline `.strip()` mismatch** — `btrim` only
  trims spaces; replaced with `~ '^\s*$'` so a `"\t"`-query row can't be
  pipeline-skipped yet sweep-invisible. FakeConn already mirrored `.strip()`.
- **medium · unenforced/racy `growth_matches_changed` count snapshot** — removed
  entirely (two reviewers converged: the per-row re-checked UPDATE predicate IS the
  no-clobber guarantee; a post-hoc table-wide count races concurrent
  `mint_interest_ladder` writes and proves nothing extra).
- **medium · dead `ActiveInterestSet.skipped_unknown_count`** — dropped; unknown skips
  stay visible via the existing `active_interest_set_built` summary field.
- **low · qualifier presence check false-positives across word boundaries** — now
  word-bounded regex ("sport" no longer "found" inside "Transportation").
- **low · `ROOT_QUERIES` forks the 2026-06-16 root map** — supersession note added:
  future queryless roots are curated in the sweep script only.

## Deferred (advisory / accepted, no fix)
- **low · `apply_backfill` mirrors `backfill_interest_query.apply_backfill`
  near-verbatim** (third copy of the asyncpg `"UPDATE <n>"` tag-parse idiom).
  Accepted mirror — extracting a shared helper across one-shot seed scripts adds
  coupling for two ~15-line functions; revisit if a fourth copy appears.
- **low · plan/write label-drift race** — `interest_label`/`depth_level` read at plan
  time could theoretically change before the UPDATE (which re-checks only the query
  column). Single-operator script, labels effectively immutable; not worth widening
  the UPDATE predicate.
- **low · `derive_query` re-implements the root-present guard `build_search_query`
  owns** — deliberate: the sweep ALWAYS qualifies deeper nodes (context-free minted
  segments), while `build_search_query` qualifies only generic labels; the two checks
  differ in policy, not by accident (comment in code explains).
