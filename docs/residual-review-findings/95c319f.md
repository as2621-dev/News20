# Residual review findings — slice #8 (section rendering), reviewed diff 95c319f

Multi-agent review panel (correctness / simplicity / contract lenses) on the slice #8
commit. Applied findings landed in the follow-up `fix(reel)` commit; the items below
were **deferred or rejected** and are recorded here so they are not session-only data
(Rule 12).

## Deferred (needs a human call)

1. **Embed-failure resilience in `getDailyFeed`** (correctness lens, low).
   If the `matched_interest:interests!feed_matched_interest_id(interest_label)` embed
   ever fails server-side (stale PostgREST schema cache after an asyncpg-applied
   migration, FK rename), the whole select errors → `getReelFeed` catches → the entire
   personalized reel renders EMPTY while all tests stay green. Proposed fix: on an
   embed-shaped error, retry the select without `matched_interest` (headers degrade to
   category-named honesty lines) before failing the feed. Deferred because
   degrade-vs-fail-loud is a product/behaviour decision: a silently degraded honesty
   line has its own honesty cost. Decide, then implement (~10 lines + one test).

2. **PRD twin-rule carve-out** (contract lens, advisory). `plans/prd.md` ("Feed
   contract twin rule holds") should be amended to carve out read-time JOIN products:
   `Story.feed_matched_interest_label` is derived by the TS read (interests embed) and
   has no `daily_feeds` column, so a Python `AllocatedSlot` twin is impossible by
   construction. Doc-only change to the PRD; owner owns that file's wording.

## Rejected (with reason — conflicting reviewer guidance resolved per Rule 7)

3. **"Drop the honesty line when a climbed slot's matched label is missing"**
   (correctness lens). Rejected the *drop*: silent substitution is the owner's
   explicitly rejected failure mode; a category-named notice ("…here's Sport") is
   truthful-but-vague (the category root IS an ancestor of the section). Applied the
   evidenced half instead: the degraded naming now logs
   `reel_section_fallback_source_unnamed` with a `fix_suggestion`.

4. **"Narrow `matched_interest` to object-only"** (contract lens) vs **"extract a
   shared normalize helper"** (simplicity lens). Kept both-shapes acceptance and
   extracted `firstOf()` — the file-wide convention (`StoryRow.segments`,
   `DailyFeedRow.stories`) deliberately accepts object|array for robustness to
   PostgREST relationship detection and the offline mocks; narrowing one embed would
   fork that convention.

5. **"Revert the `SegmentKey` one-line reformat"** (simplicity lens). Rejected:
   current Biome config enforces the one-line form — reverting reintroduces a lint
   failure in a file the slice legitimately touches.

## Flags (green tests ≠ proof — reviewer-mandated notes)

- The PostgREST embed is locked only by a select-string assertion + an intercepted
  e2e; no test hits real PostgREST. First live batch after deploy should eyeball one
  climbed slot (finding 1 covers the failure mode).
