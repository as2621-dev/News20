# Residual review findings — 1a92c4e (issue #40, chat UX contract)

## Panel outcome (Rule 12 disclosure)

Three reviewer sub-agents (correctness/logic, simplicity/reuse, contract/architecture+state)
were spawned for the post-commit panel but did not report back within ~15 minutes; the
residual gate was run by the building agent instead, lens by lens, with the riskiest
assumptions re-verified against source (not from memory):

- **Contract:** `answer_request_failed` is client-only by construction — `parseQuestionAnswer`
  (src/lib/qa/askQuestion.ts) builds the returned object from three explicit fields, so a server
  body can never set it; `buildAskAboutStoryHandler` (src/lib/voice/storyQaTool.ts) relays an
  explicit `{answer_text, answer_is_grounded, answer_citations}` object, so the flag never leaks
  into a Gemini Live tool response; failed turns are never appended to `completedTurns`, so the
  flag never enters `qaHistoryStore` localStorage. Old stored threads (field absent) parse fine.
- **Correctness:** failed-turn state machine (clear-on-new-ask, retry re-asks verbatim, no
  persistence), voice save/rehydrate story-change guard (`turnsStoryIdRef`), and pinned-scroll
  behavior are each covered by vitest (askSheetTypeThread, askSheetVoiceThread,
  useBottomAnchoredScroll, voiceTranscriptSession) AND a real-browser e2e
  (tests/e2e/chatUxContract.e2e.mjs, scenarios A–D). Issue #37's vitest + e2e suites re-run green.
- **Simplicity:** the shared hook has exactly two consumers; the session store mirrors
  `qaHistoryStore`'s bounded-eviction posture minus persistence. No speculative abstractions.

## Advisory (no-fix-now, human call)

1. **low · src/components/onboarding/InterviewChat.tsx** — the onboarding chat has its own
   scroll handling and does not use `useBottomAnchoredScroll`. Unifying was out of slice scope
   (Rule 3: surgical changes). If its autoscroll ever exhibits the yank bug, migrate it to the
   shared hook.
2. **low · voice thread rehydration turn-merge** — if a voice session ends with a USER turn and
   the sheet is reopened, the next user delta appends to that stored turn (same-role merge). In
   practice the reconnect greeting makes the model speak first, breaking the merge; cosmetic
   only. Fix if observed: stamp a session id per turn and only merge within a session.
